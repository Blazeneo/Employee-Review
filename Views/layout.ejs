<!DOCTYPE html>
<html>

<head>
    <title><%= (typeof title != "undefined" ? title : "") %></title>
    <link rel="stylesheet" href="/static/css/home.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-mono bg-gradient-to-r from-cyan-300 to-sky-500 text-white ">
    
    <div class="flex justify-between  mx-2 p-2 ">
        <% if(locals.user) { %>
        <h1 class="text-2xl font-bold text-white"><a href="<%= (mode === 'Admin' ? '/admin/home' : '/home') %>" class=" rounded-md w-30 px-1 focus:bg-cyan-400  "><%= locals.user.name %></a> </h1>
        <% if(mode === 'Admin') { %>
        <a href="/users/admin/performance" class=" rounded-md w-30 px-1 focus:bg-cyan-300 hover:bg-sky-400 ">Employee review</a>

        <% } %>
        <a href="/users/logout" class=" rounded-md w-14 px-1 focus:bg-cyan-300 hover:bg-sky-400 ">Logout</a>
        <% } %>
    </div>

    <!-- Main Content Area -->
    <div id="container" class="font-mono bg-gradient-to-r from-cyan-500 to-green-300 h-screen ">
        <%- body %>
    </div>
</body>

</html>
